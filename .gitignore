# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Vercel
.vercel

# Env / local database
# `.env` alone matches only that exact name — every sibling (.env.local, .env.production,
# .env.backup-*, …) would otherwise be committed with live credentials in it.
.env
.env.*
!.env.example
mediOS.db
mediOS.db-*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
