# Copy this file to .env and fill in your values.

# Required for AI-powered purchase invoice OCR (Purchase Entry module).
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# PostgreSQL connection string. For local dev, run:
#   docker run -d --name mediOS-postgres -e POSTGRES_PASSWORD=mediospass -e POSTGRES_DB=mediOS -p 5433:5432 postgres:16
# then use the URL below (adjust host/port/credentials for your own Postgres instance).
DATABASE_URL=postgres://postgres:defaultdb@pg-60963f5-programmer62563-baa7.k.aivencloud.com:18702/defaultdb
